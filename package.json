{
  "name": "blog-ia",
  "version": "0.1.0",
  "private": true,
  "description": "Blog estatico para publicar Jesus y su agente IA.",
  "scripts": {
    "build": "node scripts/build.js",
    "check": "node --check scripts/build.js && node --check scripts/serve.js && node --check scripts/build.test.js && node --check public/admin.js",
    "test": "node --test scripts/build.test.js",
    "start": "node scripts/serve.js"
  },
  "engines": {
    "node": ">=18"
  }
}
